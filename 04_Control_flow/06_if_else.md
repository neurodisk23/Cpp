**The form of C++ 'if-else', 'if-else if ' statements is:**

```

if(boolean expression)
{
     //statements to execute if the boolean expression is true
}
else
{
     //statements to execute if the boolean expression is false
}
```
